Files:

Matrix_Spiral.py - Creates a square matrix from numbers written down in spiral that starts from upper-left corner and ends in center of the matrix.

Matrix_Zero_Memory.py - Creates a N*M matrix in form of an one-dimensional array from numbers of rows and columns that user defines. Than it makes a matrix transposition without using any additional memory (without additional matrix or array).
